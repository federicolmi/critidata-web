# Binarios pesados — hostear vía GitHub Releases u otro CDN
CritiData_Nefrologia.exe
*.exe

# Variables de entorno sensibles — excluir si contienen secrets
# app/assets/.env  ← la URL del API es pública, se incluye en el repo

# CanvasKit — Flutter lo carga desde el CDN de Google, no hace falta en el repo
app/canvaskit/

# Sistema
.DS_Store
Thumbs.db
